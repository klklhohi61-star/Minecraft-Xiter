services:
  - type: web
    name: minecraft-xiter-bot
    runtime: node
    buildCommand: npm install
    startCommand: node index.js
    plan: free
    healthCheckPath: /health
    envVars:
      - key: BOT_HOST
        sync: false          # your server address, e.g. my-server.falixsrv.me
      - key: BOT_PORT
        value: "25565"
      - key: BOT_USERNAME
        value: XiterBot
      - key: MC_VERSION
        value: "1.20.1"
      - key: BOT_AUTH
        value: offline       # change to 'microsoft' for premium accounts
      - key: RECONNECT_DELAY
        value: "10000"
      - key: AFK_INTERVAL
        value: "30000"
      - key: CHAT_PREFIX
        value: "!"
      - key: OWNER_USERNAME
        sync: false          # your in-game username for !stop / !reconnect
